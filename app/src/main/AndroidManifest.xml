<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.aorora.app"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".base.APP"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppThemes">
        <activity android:name=".MainActivity">
        </activity>
        <!--
        <activity
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="false"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:name=".base.BaseActivity">
        </activity>
        -->
        <activity android:name=".membercenter.AccountActivity">
        </activity>
        <activity android:name=".home.GiftActivity">
        </activity>
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".home.IntegelActivity">
        </activity>
        <activity android:name=".home.WebViewActivity">
        </activity>
        <activity android:name=".home.ProductInfoActivity">
        </activity>
        <activity android:name=".cluster.MemberListActivity">
        </activity>
    </application>

</manifest>